import { OrderConditionPipe } from './order-condition.pipe';

describe('OrderConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
